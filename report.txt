PR Risk Report — {owner}/{repo}
Generated: {timestamp}
============================================================

PR #{parsed['number']} — {parsed['title']}
  Author       : {parsed['author']}
  Files changed: {parsed['files_changed']}
  Additions    : +{parsed['additions']}
  Deletions    : -{parsed['deletions']}
  Risk Level   : {risk}
  Summary      : {summary}

PR #{parsed['number']} — {parsed['title']}
  Author       : {parsed['author']}
  Files changed: {parsed['files_changed']}
  Additions    : +{parsed['additions']}
  Deletions    : -{parsed['deletions']}
  Risk Level   : {risk}
  Summary      : {summary}

PR #{parsed['number']} — {parsed['title']}
  Author       : {parsed['author']}
  Files changed: {parsed['files_changed']}
  Additions    : +{parsed['additions']}
  Deletions    : -{parsed['deletions']}
  Risk Level   : {risk}
  Summary      : {summary}
